name: task_builder
description: Builder for Android projects.
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  collection: ^1.19.0
  equatable: ^2.0.7
  path: ^1.9.0
  rxdart: ^0.28.0
  storage:
    path: ../storage/storage
  task_repository:
    path: ../task_repository
  preferences_repository:
    path: ../preferences_repository

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.0
  mocktail: ^1.0.0
  very_good_analysis: ^7.0.0
